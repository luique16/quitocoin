databases:
  - name: postgres-inst
    databaseName: appdb
    user: appuser
    region: oregon

services:
  - type: redis
    name: redis-inst
    region: oregon
    plan: free
    ipAllowList: []

  - type: web
    name: backend-go
    env: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres-inst
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: redis-inst
          property: connectionString
      - key: PORT
        value: "8080"

  - type: web
    name: frontend-next
    env: docker
    dockerfilePath: ./public/Dockerfile
    region: oregon
    dockerCommand: sh -c "npm start"
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: backend-go
          property: host
      - key: NEXT_PUBLIC_MINING_DIFFICULTY
        value: "7"
